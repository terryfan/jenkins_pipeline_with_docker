#!/usr/bin/env groovy

pipeline {
	agent {
		dockerfile {
			filename 'Dockerfile_pre'
		}
	}
	stages {
		stage('build') {
			steps {
				withEnv(['PWD=' + pwd()]){
					sh 'echo hello world! $(PWD)'
				}
			}
		}		
	}
}